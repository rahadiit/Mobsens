time,longitude,latitude,altitude,speed,bearing,accuracy
<% @locations.each do |b|%>
<%= b.get_milliseconds %>,<%= b.longitude %>,<%= b.latitude %>,<%= b.altitude %>,<%= b.speed %>,<%= b.bearing %>,<%= b.accuracy %>
<% end %>

